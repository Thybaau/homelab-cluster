{{- if and .Values.networkPolicy.enabled .Values.networkPolicy.defaultDeny.enabled }}
# NetworkPolicy default-deny : bloque tout trafic ingress dans le namespace homepage.
# Les flux légitimes sont autorisés par des NetworkPolicies spécifiques.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "homepage.fullname" . }}-default-deny
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "homepage.labels" . | nindent 4 }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
{{- end }}
