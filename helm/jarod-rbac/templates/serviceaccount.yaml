{{- if .Values.serviceAccount.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jarod-rbac.serviceAccountName" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "jarod-rbac.labels" . | nindent 4 }}
{{- end }}
