{{- if and .Values.networkPolicy.enabled .Values.networkPolicy.allowProbes.enabled }}
# NetworkPolicy allow-probes : autorise le trafic ingress depuis blackbox-exporter (namespace monitoring)
# vers cloudflared (probes HTTP).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "cloudflare.fullname" . }}-allow-probes
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "cloudflare.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "cloudflare.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 2000
{{- end }}
